package com.bookmyshow.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {
    // Routes are dynamically configured via application.properties
}
